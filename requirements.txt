# HermesAgentEvolution 依赖列表
# sqlite3 为 Python 内置模块，无需额外安装

## 核心依赖（Python 3.9+ 内置）
# - sqlite3 (Python标准库)
# - json (Python标准库)
# - datetime (Python标准库)
# - dataclasses (Python标准库)

## 可选依赖
# requests>=2.25.0       # HTTP客户端（用于飞书通知）
# numpy>=1.19.0          # 数值计算（用于高级分析）
# scikit-learn>=0.24.0   # 机器学习（用于模式识别）

## 开发依赖
pytest>=7.0.0            # 测试框架
pytest-cov>=4.0.0        # 测试覆盖率
